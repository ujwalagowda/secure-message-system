/**
 * config package
 *
 * Contains Spring configuration classes:
 *   - SecurityConfig.java       Spring Security rules (permit/restrict routes, JWT filter)
 *   - CorsConfig.java           CORS configuration to allow the frontend origin
 *
 * These classes will be implemented in Phase 3.
 */
package com.securemsg.config;
